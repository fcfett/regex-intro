import React from 'react';

export default () => (
  <div id="page-11" className="page">
    <h1>Quotes</h1>
    <ul className="list">
      <li>Cada elemento na regex se refere a um determinado caractere de uma string</li>
      <li>Assim como no desenvolvimento, há vária formas de implementar</li>
      <li>Tua regex vai retornar exatamente o que tu pede</li>
    </ul>
  </div>
);
